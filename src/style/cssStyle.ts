import CSS from 'csstype';

const card: CSS.Properties = {
    marginBlock: '5rem',
    minWidth:'90rem'
}

const title: CSS.Properties = {
    fontSize: '45px',
    display: 'flex',
    alignItems: 'start',
    marginLeft: '1rem'
}

export {card, title};